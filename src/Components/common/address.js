import React from 'react'

const Address = (props) => {



    return (
        <div>
            {/* {props.findLocation(props.image)
                .then(response => {
                    return (
                        <p>{response.data.address.city}</p>
                    )
                })
            } */}
            <p>location</p>
        </div>
    );
}

export default Address;